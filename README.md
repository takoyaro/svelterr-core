# Svelterr-core

Display Svelte-Kit client-side errors as they happen.

## Shouldn't be installed standalone

You shouldn't install this package on it's own.
Use `npx svelterr` in your SvelteKit project root directory.